name                = "budget-worker"
main                = "src/index.js"
compatibility_date  = "2024-01-01"

[[kv_namespaces]]
binding    = "BUDGET_KV"
id         = "SOSTITUISCI_CON_PRODUCTION_ID"
preview_id = "SOSTITUISCI_CON_PREVIEW_ID"

# Cron per backup notturni automatici (opzionale)
# [triggers]
# crons = ["0 3 * * *"]
